#include <iostream>
 
using namespace std;
 
int main() {
	int i,k;
	string str;
	cin>>str;
	for(i=0;i<str.length();i++){
	    k= str[i];
	    if(k>=65 && k<=90){
	        k += 32;
	    }
	    else if(k>=97 && k<=122){
	        k -= 32;
	    }
	    str[i]=k;
	}
	cout<<str;
}
Language: C++
